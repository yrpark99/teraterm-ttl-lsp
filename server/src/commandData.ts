/**
 * Auto-generated TTL command metadata (name, category, format, summary).
 *
 * The "format" and "summary" text is distilled from the "Format" and "Remarks"
 * sections of the official Tera Term command reference:
 *   https://teratermproject.github.io/manual/5/en/macro/command/
 *
 * DO NOT EDIT BY HAND. Edit scripts/command-data/*.json and run:
 *   npm run gen:commands
 */

/** [name, category, format, summary] */
export type CommandTableEntry = [string, string, string, string];

export const COMMAND_TABLE: ReadonlyArray<CommandTableEntry> = [
  ["bplusrecv", "Communication command", "bplusrecv", "Receives a file from the host using the B-Plus protocol. Blocks until the transfer completes; sets result to 1 on success, 0 on failure. Files are saved to the file transfer folder."],
  ["bplussend", "Communication command", "bplussend <filename>", "Sends <filename> to the host using the B-Plus protocol. Blocks until the transfer completes; sets result to 1 on success, 0 on failure. Relative paths resolve against the file transfer folder."],
  ["callmenu", "Communication command", "callmenu <menu ID>", "Executes the Tera Term menu item identified by <menu ID> (IDs are listed in the Keycode reference)."],
  ["changedir", "Communication command", "changedir <path>", "Changes Tera Term's file transfer directory to <path>. Relative paths in file transfer commands resolve against it. Differs from setdir, which changes the macro's working directory."],
  ["clearscreen", "Communication command", "clearscreen <int>", "Clears the screen depending on <int>: 0 clears the VT window, 1 clears the scroll buffer and VT window, 2 clears the TEK window."],
  ["closett", "Communication command", "closett", "Closes Tera Term and puts the macro in an unlinked state, suppressing TCP/IP disconnect confirmation. A later connect command can open a new Tera Term and re-link the macro."],
  ["connect", "Communication command", "connect <command line parameters>", "Links the macro to Tera Term and optionally connects to a host with the given command-line parameters. Sets result to 0 (no link), 1 (linked, not connected), or 2 (linked and connected); ignored if already linked and connected."],
  ["cygconnect", "Communication command", "cygconnect [<command line parameters>]", "Connects to Cygwin (CygTerm) via Tera Term, passing the given parameters to CygTerm. Sets result to 0 (no link), 1 (linked, not connected), or 2 (linked and connected); ignored if already linked and connected."],
  ["disconnect", "Communication command", "disconnect [<confirm>]", "Closes the connection between Tera Term and the host. <confirm> 0 disconnects without a confirmation dialog; nonzero (default) shows the dialog. The macro-to-Tera Term link is kept."],
  ["dispstr", "Communication command", "dispstr <data1> [<data2>....]", "Displays the given strings or characters on the Tera Term terminal screen; integer values are shown as the ASCII character of their lowest byte. Output is local only and may desync the buffer from the host. Escape sequences are supported."],
  ["enablekeyb", "Communication command", "enablekeyb <flag>", "Enables (<flag> 1) or disables (<flag> 0) user keyboard input during macro execution; when disabled the user cannot type while the macro sends data."],
  ["flushrecv", "Communication command", "flushrecv", "Clears the MACRO receive buffer that holds characters received from the host, discarding stale data so it does not interfere with later character-reading commands such as wait."],
  ["gethostname", "Communication command", "gethostname <strvar>", "Stores the destination hostname or IP address of Tera Term into the string variable <strvar>."],
  ["getmodemstatus", "Communication command", "getmodemstatus <intvar>", "Stores serial port modem control-register signal status into <intvar> as a bit value: CTS(1), DSR(2), ring indicator(4), RLSD(8). Sets result to 0 on success, 1 on failure."],
  ["gettitle", "Communication command", "gettitle <strvar>", "Stores the Tera Term window title text into the string variable <strvar>."],
  ["getttpos", "Communication command", "getttpos <showflag> <window x> <window y> <window width> <window height> <client x> <client y> <client width> <client height>", "Retrieves the VT window's show state (normal/minimized/maximized/hidden) and the pixel position and size of the window and client areas into the given variables. Sets result to 0 on success, -1 on failure."],
  ["kmtfinish", "Communication command", "kmtfinish", "Tells Tera Term to perform the Kermit Finish operation. Blocks until it completes; sets result to 1 on success, 0 otherwise."],
  ["kmtget", "Communication command", "kmtget <filename>", "Retrieves <filename> from the host using the Kermit Get protocol (host must be in server mode). Blocks until done; sets result to 1 on success, 0 on failure. Relative paths save to the file transfer folder."],
  ["kmtrecv", "Communication command", "kmtrecv", "Receives a file using the Kermit protocol. Blocks until the transfer completes; sets result to 1 on success, 0 on failure. Files are saved to the file transfer folder."],
  ["kmtsend", "Communication command", "kmtsend <filename>", "Sends <filename> to the host using the Kermit protocol. Blocks until the transfer completes; sets result to 1 on success, 0 on failure. Relative paths resolve against the file transfer folder."],
  ["loadkeymap", "Communication command", "loadkeymap <filename>", "Tells Tera Term to load the keyboard setup (keymap) file specified by <filename>."],
  ["logautoclosemode", "Communication command", "logautoclosemode <flag>", "Controls whether an open log file is closed automatically when the macro ends: <flag> 1 enables, 0 disables. The setting reverts to its default when the macro terminates."],
  ["logclose", "Communication command", "logclose", "Closes the currently open log file, ending the logging started by logopen."],
  ["loginfo", "Communication command", "loginfo <strvar>", "Stores the current log filename into <strvar> and sets result to a bitwise flag combination of the log settings (binary 1, append 2, plain text 4, timestamp 8, hide dialog 16), or -1 if logging is not active."],
  ["logopen", "Communication command", "logopen <filename> <binary flag> <append flag> [<plain text flag> [<timestamp flag> [<hide dialog flag> [<include screen buffer flag> [<timestamp type>]]]]]", "Starts logging received characters to <filename>. Binary flag writes data unmodified; append flag appends vs. overwrites; optional flags control plain text, timestamps, dialog hiding, screen buffer, and timestamp type. Sets result to 0 on success, 1 on failure."],
  ["logpause", "Communication command", "logpause", "Temporarily suspends logging; characters received while paused are discarded. Logging can be resumed with logstart."],
  ["logrotate", "Communication command", "logrotate 'size' '<size>'\nlogrotate 'rotate' <count>\nlogrotate 'halt'", "Configures automatic log file rotation by file size, sets how many rotated copies to keep, or halts rotation. A log file must already be open; rotation occurs automatically, not immediately."],
  ["logstart", "Communication command", "logstart", "Resumes logging that was suspended by logpause, continuing to record to the log file."],
  ["logwrite", "Communication command", "logwrite <string>", "Appends <string> to the log file. Works only while a log is open (including while paused); an empty string causes a syntax error."],
  ["quickvanrecv", "Communication command", "quickvanrecv", "Receives a file from the host using the Quick-VAN protocol, pausing until the transfer completes; sets system variable result to 1 on success, 0 otherwise. Files are saved to the file transfer folder."],
  ["quickvansend", "Communication command", "quickvansend <filename>", "Sends a file to the host using the Quick-VAN protocol, pausing until the transfer completes; sets result to 1 on success, 0 otherwise. Relative paths resolve to the file transfer folder."],
  ["recvln", "Communication command", "recvln", "Receives one line of characters and stores it in the system variable inputstr, blocking until a line is received, the connection ends, or a timeout occurs. Sets result to 1 on success, 0 otherwise; typically used in a loop after setsync 1."],
  ["recvfile", "Communication command", "recvfile <filename> <binary flag> <auto-stop wait time>", "Writes data received from the host to a file unchanged (binary mode fixed, no conversions). If no data arrives within the wait time (seconds), reception stops and result is set to 1, otherwise 0; relative paths use the file transfer folder."],
  ["restoresetup", "Communication command", "restoresetup <filename>", "Causes Tera Term to load the Tera Term setup file specified by <filename>."],
  ["scprecv", "Communication command", "scprecv <remote filename> [<local filename>]", "Receives a remote file from the host using the SCP protocol. If no local filename is given, the file is copied to the local current directory. Runs asynchronously (does not wait). Available in version 4.57 or later."],
  ["scpsend", "Communication command", "scpsend <filename> [<destination filename>]", "Sends a local file to the remote host using the SCP protocol. If no destination is given, the file is copied to the remote home directory. Runs asynchronously (does not wait). Available in version 4.57 or later."],
  ["send", "Communication command", "send <data1> <data2>....", "Sends characters to the host. String data is transmitted as text; an integer's lowest byte (0-255) is sent as an ASCII character. Text is sent like sendtext, binary like sendbinary."],
  ["sendbinary", "Communication command", "sendbinary <data1> <data2>....", "Sends characters to the host without modification (strings sent as UTF-8, an integer's lowest byte sent as an ASCII character). Data is forwarded unchanged with no character-code conversion; use sendtext to convert codes."],
  ["sendbreak", "Communication command", "sendbreak", "Sends a break signal to the host. Raises a break on serial consoles, sends IAC BREAK (255 243) on TELNET, and SSH2_MSG_CHANNEL_REQUEST(break) on SSH2."],
  ["sendbroadcast", "Communication command", "sendbroadcast <data1> <data2>....", "Broadcasts characters to all terminals and then sends them to the host (strings as-is, integer's lowest byte as ASCII). Data is ignored on terminals where AcceptBroadcast is disabled. Available in version 4.62 or later."],
  ["sendfile", "Communication command", "sendfile <filename> <binary flag>", "Sends a file to the host and waits for the transfer to complete. A non-zero binary flag sends the file unchanged; zero converts newlines (CR to CR/CRLF) and removes control characters except TAB, LF, CR. Relative paths use the file transfer folder."],
  ["sendkcode", "Communication command", "sendkcode <key code> <repeat count>", "Performs the function defined for a key or key combination (key code per KEYCODE.EXE) the given number of times, typically sending a string to the host. Cannot run macros (control flag 2) or menu commands (control flag 3); use callmenu for menu commands."],
  ["sendln", "Communication command", "sendln <data1> <data2>....", "Sends characters followed by a newline to the host; data format is the same as send. May be called with no arguments to send only a newline."],
  ["sendlnbroadcast", "Communication command", "sendlnbroadcast <data1> <data2>....", "Broadcasts characters followed by a newline to all terminals and then sends them to the host; data format is the same as sendbroadcast. Data is ignored on terminals where AcceptBroadcast is disabled."],
  ["sendlnmulticast", "Communication command", "sendlnmulticast <multicastname> <data1> <data2>....", "Multicasts characters followed by a newline to the terminals selected by <multicastname> and then sends them to the host; data format is the same as sendmulticast. Available in version 4.96 or later."],
  ["sendmulticast", "Communication command", "sendmulticast <multicastname> <data1> <data2>....", "Sends data to the multicast terminals named by <multicastname> (set first with setmulticastname); strings sent as-is, an integer's lowest byte as ASCII. Data is ignored on terminals where AcceptBroadcast is disabled."],
  ["sendtext", "Communication command", "sendtext <data1> <data2>....", "Sends characters to the host (strings as text, an integer's lowest byte as ASCII); the text is received and converted to character code before transmission. Use sendbinary to bypass conversion. Available in version 5.3 or later."],
  ["setbaud", "Communication command", "setbaud <value>", "Changes the serial port speed. Version 4.66 and later accept the actual baud rate (e.g. 115200); earlier versions use a speed ID number (1-15). Available in version 4.58 or later."],
  ["setdebug", "Communication command", "setdebug <flag>", "Configures Tera Term's debug mode: 0 = off (normal output), 1 = usual debug mode, 2 = hex output (received bytes printed in space-separated uppercase hex), 3 = output disabled. Available in version 4.64 or later."],
  ["setdtr", "Communication command", "setdtr <flag>", "Sets the DTR (Data Terminal Ready) line: non-zero enables, zero disables. Only works on serial connections with flow control set to none. Available in version 4.59 or later."],
  ["setecho", "Communication command", "setecho <echo flag>", "Sets local echo: a non-zero echo flag enables local echo, zero disables it."],
  ["setflowctrl", "Communication command", "setflowctrl <value>", "Changes serial flow control: 1 = Xon/Xoff, 2 = hardware RTS/CTS, 3 = none, 4 = hardware DSR/DTR (4.105+). Available in version 4.94 or later."],
  ["setmulticastname", "Communication command", "setmulticastname <multicastname>", "Sets the terminal's own multicast identification name, used as the target for multicast send commands. Available in version 4.62 or later."],
  ["setrts", "Communication command", "setrts <flag>", "Sets the RTS (Request To Send) line: non-zero enables, zero disables. Only works on serial connections with flow control set to none. Available in version 4.59 or later."],
  ["setserialdelaychar", "Communication command", "setserialdelaychar <delay>", "Changes the time interval (in milliseconds) between individual characters sent by Tera Term over serial. Available in version 5.5.0 or later."],
  ["setserialdelayline", "Communication command", "setserialdelayline <delay>", "Changes the time interval (in milliseconds) between lines sent by Tera Term over serial. Available in version 5.5.0 or later."],
  ["setspeed", "Communication command", "setspeed <value>", "Changes the serial port speed to the specified bps value; an alias for setbaud. Available in version 4.99 or later."],
  ["setsync", "Communication command", "setsync <sync flag>", "Sets synchronous (non-zero) or asynchronous (zero, the default) communication mode. Synchronous mode prevents buffer overflow by pausing reception when the buffer is full, but slows reception; use it only when needed."],
  ["settitle", "Communication command", "settitle <title>", "Changes Tera Term's window title to the string given in <title>."],
  ["showtt", "Communication command", "showtt <show flag>", "Controls window visibility/state by flag: VT window (-1 hide, 0 minimize, 1 restore), TEK window (2 hide, 3 minimize, 4 open/restore, 5 close), Log window (6 hide, 7 minimize, 8 restore)."],
  ["testlink", "Communication command", "testlink", "Reports the current link and connection status in system variable result: 0 = no link to Tera Term, 1 = linked but not connected to host, 2 = both link and connection established."],
  ["unlink", "Communication command", "unlink", "Terminates the link between the current Tera Term window and the macro, leaving the macro with no control over the window. The connect command can later link to a new window."],
  ["wait", "Communication command", "wait <string1> [<string2> ...]", "Pauses until one of up to 10 strings is received from the host, or until timeout. An empty string waits for one character. Sets result to which string matched (1-10), or 0 on timeout; timeout governed by timeout/mtimeout variables."],
  ["wait4all", "Communication command", "wait4all <string1> [<string2> ...]", "Pauses until one of up to 10 strings is received across all macro-linked terminals, or until timeout; only terminals linked when the command runs are monitored. Sets result to which string matched (1-10), or 0 on timeout. Requires Wait4allMacroCommand enabled in teraterm.ini."],
  ["waitevent", "Communication command", "waitevent <events>", "Pauses until one of the specified events occurs: 1 (timeout), 2 (unlink), 4 (disconnection), 8 (connection), combinable. Sets system variable result to the identifier of the event that occurred."],
  ["waitln", "Communication command", "waitln <string1> [<string2> ...]", "Pauses until a line containing one of up to 10 strings is received, or until timeout; an empty string waits for any line. Stores the received line in inputstr and sets result to which string matched (1-10), or 0 on timeout."],
  ["waitn", "Communication command", "waitn <received byte count>", "Pauses until the given number of bytes is received from the host, or until timeout. Stores the received characters (max 511 bytes) in inputstr and sets result to 1 on success, 0 on timeout. Available in version 4.62 or later."],
  ["waitrecv", "Communication command", "waitrecv <sub-string> <len> <pos>", "Pauses until a received string of length <len> bytes contains <sub-string> at position <pos> (1-indexed), or until timeout; stores the received string in inputstr. Sets result to 1 on match, 0 on timeout, or -1 if the substring matched but the length was short due to timeout."],
  ["waitregex", "Communication command", "waitregex <string1 with regular expression> [<string2 with regular expression> ...]", "Pauses until a received line (max 511 chars) matches one of up to 10 Oniguruma regex patterns, or until timeout. Sets result to which pattern matched (1-10) or 0 on timeout, stores the line in inputstr, the matched text in matchstr, and capture groups in groupmatchstr1-9."],
  ["xmodemrecv", "Communication command", "xmodemrecv <filename> <binary flag> <option>", "Receives a file from the host using the XMODEM protocol, pausing until transfer completes; sets result to 1 on success, 0 on failure. Binary flag non-zero for binary files; option selects Checksum/CRC/1K (option 3 = CRC with 1K packets in 4.90+). Relative paths use the file transfer folder."],
  ["xmodemsend", "Communication command", "xmodemsend <filename> <option>", "Sends a file using the XMODEM protocol, pausing until transfer completes; sets result to 1 on success, 0 on failure. Option 1 = Checksum, 2 = CRC, 3 = 1K packet size; relative paths use the file transfer folder."],
  ["ymodemrecv", "Communication command", "ymodemrecv", "Receives a file from the host using the YMODEM protocol, pausing until transfer completes; sets result to 1 on success, 0 on failure. Relative paths use the file transfer folder. Available in version 4.66 or later."],
  ["ymodemsend", "Communication command", "ymodemsend <filename>", "Sends the file <filename> to the host using the YMODEM protocol, pausing until transfer completes; sets result to 1 on success, 0 on failure. Relative paths use the file transfer folder."],
  ["zmodemrecv", "Communication command", "zmodemrecv", "Receives a file from the host using the ZMODEM protocol, pausing until transfer completes; sets result to 1 on success, 0 on failure. Relative paths use the file transfer folder."],
  ["zmodemsend", "Communication command", "zmodemsend <filename> <binary flag>", "Sends the file <filename> to the host using the ZMODEM protocol, pausing until transfer completes; sets result to 1 on success, 0 on failure. Binary flag non-zero for binary files; relative paths use the file transfer folder."],
  ["code2str", "String operation command", "code2str <strvar> <ASCII code>", "Converts an ASCII code value into its corresponding character string and stores it in <strvar>. Multi-byte codes produce up to 4 characters; a zero input sets <strvar> to an empty string."],
  ["expandenv", "String operation command", "expandenv <strvar> [<strval>]", "Expands environment variable references written as %name% to their values. With one argument it expands <strvar> in place; with two it expands <strval> and stores the result in <strvar>. Unknown variables are left unchanged."],
  ["int2str", "String operation command", "int2str <strvar> <integer value>", "Converts <integer value> to its string representation and returns it in the string variable <strvar>. Equivalent to sprintf with the %d specifier."],
  ["regexoption", "String operation command", "regexoption <option1> [<option2> ...]", "Configures regex behavior for strmatch, strreplace, and waitregex: syntax style (Ruby/Perl/POSIX/Java), character encoding (UTF-8, ASCII, etc.), and options like case-insensitive or multiline. Defaults are Ruby syntax and UTF-8."],
  ["sprintf", "String operation command", "sprintf FORMAT [ARGUMENT ...]", "Produces C printf-style formatted output and stores the result in the system variable inputstr. Sets result to 0 on success, 1 if format is missing, 2 for invalid format, or 3 for invalid arguments."],
  ["sprintf2", "String operation command", "sprintf2 strvar FORMAT [ARGUMENT ...]", "Produces C printf-style formatted output and stores the result in the variable strvar. Sets result to 0 on success, 1 if format is missing, 2 for invalid format, 3 for invalid argument, or 4 for invalid destination variable."],
  ["str2code", "String operation command", "str2code <intvar> <string>", "Converts <string> to its ASCII code value(s) and stores the result in the integer variable <intvar>. Holds at most 4 ASCII codes; if the string is longer, only the last 4 bytes are copied."],
  ["str2int", "String operation command", "str2int <intvar> <string>", "Converts <string> to its numeric value (decimal, or hexadecimal as 0x7b or $7b) and stores it in <intvar>. Sets result to 1 on success or 0 on failure; only the leading numeric portion is converted."],
  ["strcompare", "String operation command", "strcompare <string1> <string2>", "Performs a lexicographic comparison of two strings and stores the outcome in result: -1 if string1 < string2, 0 if equal, or 1 if string1 > string2."],
  ["strconcat", "String operation command", "strconcat <strvar> <string>", "Appends a copy of <string> to the end of the string variable <strvar>, modifying it in place with the combined result."],
  ["strcopy", "String operation command", "strcopy <string> <pos> <len> <strvar>", "Extracts a substring from <string> starting at position <pos> (1-based) of length <len> and stores the result in the string variable <strvar>."],
  ["strinsert", "String operation command", "strinsert <strvar> <index> <string>", "Inserts <string> into the string variable <strvar> at position <index> (1-based; position 1 is before the first character). Indices outside 1 to length+1 cause a syntax error."],
  ["strjoin", "String operation command", "strjoin <strvar> <separator> [<count>]", "Concatenates the system variables groupmatchstr1 through groupmatchstr9 into one string, inserting <separator> between elements, and stores it in <strvar>. <count> limits how many are joined (default 9)."],
  ["strlen", "String operation command", "strlen <string>", "Counts the number of bytes in <string> and stores the count in the system variable result. An empty or null string yields 0."],
  ["strmatch", "String operation command", "strmatch <target string> <string with regular expression>", "Matches <target string> against a regular expression. Sets result to 0 for no match or the matched offset (1-origin) on a match; the matched text goes to matchstr and captured groups to groupmatchstr1 through groupmatchstr9."],
  ["strremove", "String operation command", "strremove <strvar> <index> <len>", "Deletes <len> characters from the string variable <strvar> starting at position <index> (1-based), modifying the variable in place."],
  ["strreplace", "String operation command", "strreplace <strvar> <index> <regex> <newstr>", "Replaces the first regex match in <strvar> starting at <index> (1-origin) with <newstr> (deleting the match if <newstr> is empty). Sets result to 1 on success (matchstr holds the match), 0 if no match, or -1 for invalid regex."],
  ["strscan", "String operation command", "strscan <string> <substring>", "Searches for <substring> within <string> and stores the position of the first occurrence (1-based) in result, or 0 if not found."],
  ["strspecial", "String operation command", "strspecial <strvar> [<strval>]", "Converts escape sequences such as backslash-n and backslash-t into their special characters. With one argument it converts <strvar> in place; with two it converts <strval> and stores the result in <strvar>."],
  ["strsplit", "String operation command", "strsplit <strval> <separator> [<count>]", "Splits <strval> by the delimiter <separator> into the system variables groupmatchstr1 through groupmatchstr9. <count> (max 9) limits the number of pieces; result holds the actual number of divisions (10 when more than 9 with count omitted)."],
  ["strtrim", "String operation command", "strtrim <strvar> <trimchars>", "Removes all characters in the set <trimchars> from the beginning and end of the string variable <strvar>, storing the trimmed result back in <strvar>."],
  ["tolower", "String operation command", "tolower <strvar> <string>", "Converts all alphabetic characters in <string> to lowercase and returns the result in the string variable <strvar>; non-alphabetic characters are unchanged."],
  ["toupper", "String operation command", "toupper <strvar> <string>", "Converts all alphabetic characters in <string> to uppercase and returns the result in the string variable <strvar>; non-alphabetic characters are unchanged."],
  ["basename", "File operation command", "basename <strvar> <path>", "Extracts the trailing name component (file or directory name) from <path> and stores it in <strvar>. The path need not exist."],
  ["dirname", "File operation command", "dirname <strvar> <path>", "Removes the filename portion of <path> and stores the resulting directory path in <strvar>. The path need not exist."],
  ["fileclose", "File operation command", "fileclose <file handle>", "Closes the file specified by <file handle>. The handle is no longer valid after this command."],
  ["fileconcat", "File operation command", "fileconcat <file1> <file2>", "Appends a copy of <file2> to the end of <file1>; the two files must not be the same. Sets system variable result to 0 on success, non-zero on failure."],
  ["filecopy", "File operation command", "filecopy <file1> <file2>", "Copies <file1> to <file2>, overwriting <file2> if it exists; the files must differ. Sets system variable result to 0 on success, non-zero on failure."],
  ["filecreate", "File operation command", "filecreate <file handle> <filename>", "Creates and opens <filename>, truncating it to zero if it exists, with the pointer at the start. Stores a positive integer handle in <file handle> on success or -1 on failure, and sets result to 0 on success or non-zero on failure."],
  ["filedelete", "File operation command", "filedelete <filename>", "Deletes the file specified by <filename>. Sets system variable result to 0 on success, non-zero on failure."],
  ["filelock", "File operation command", "filelock <file handle> [<timeout>]", "Acquires an exclusive lock on the file for the calling process. <timeout> (seconds) controls waiting: 0 returns immediately, omitted waits forever. Sets result to 0 on success, 1 on failure."],
  ["filemarkptr", "File operation command", "filemarkptr <file handle>", "Marks the current file pointer of the open file specified by <file handle>. The marked position can be recalled with the fileseekback command."],
  ["fileopen", "File operation command", "fileopen <file handle> <filename> <append flag> [<readonly flag>]", "Opens <filename> in binary mode, creating it if absent. Stores the handle in <file handle> on success or -1 on failure. <append flag> nonzero puts the pointer at the end; <readonly flag> nonzero opens read-only."],
  ["fileread", "File operation command", "fileread <file handle> <read byte> <strvar>", "Reads <read byte> bytes (1-511) from the open file into <strvar>, advancing the pointer. Sets result to 1 if end of file is reached, otherwise 0."],
  ["filereadln", "File operation command", "filereadln <file handle> <strvar>", "Reads a line from the open file into <strvar>, advancing the pointer to the next line. Sets result to 1 if end of file is reached during the read, otherwise 0."],
  ["filerename", "File operation command", "filerename <file1> <file2>", "Renames <file1> to <file2>; the two names must not be the same. Sets system variable result to 0 on success, non-zero on failure."],
  ["filesearch", "File operation command", "filesearch <filename>", "Searches for the file or folder specified by <filename>. Sets system variable result to 1 if found, 0 if not found."],
  ["fileseek", "File operation command", "fileseek <file handle> <offset> <origin>", "Moves the file pointer by <offset> from a position set by <origin>: 0 = beginning of file, 1 = current position, 2 = end of file. Offset may be positive or negative."],
  ["fileseekback", "File operation command", "fileseekback <file handle>", "Moves the file pointer of the open file to the position previously marked by the filemarkptr command."],
  ["filestat", "File operation command", "filestat <filename> <size> [<mtime>] [<drive>]", "Retrieves information about a file or directory: stores its size in bytes in <size>, last modification time in <mtime>, and drive number in <drive>. Sets result to -1 on error."],
  ["filestrseek", "File operation command", "filestrseek <file handle> <string>", "Searches forward from the current pointer in the open file for <string>. If found, moves the pointer just after the string and sets result to 1; otherwise leaves the pointer and sets result to 0."],
  ["filestrseek2", "File operation command", "filestrseek2 <file handle> <string>", "Searches backward from the current pointer in the open file for <string>. If found, moves the pointer to just before the string and sets result to 1; otherwise leaves the pointer and sets result to 0."],
  ["filetruncate", "File operation command", "filetruncate <filename> <size>", "Resizes <filename> to <size> bytes, padding with null characters when extending. Sets system variable result to 0 on success, -1 on error."],
  ["fileunlock", "File operation command", "fileunlock <file handle>", "Unlocks the whole file, enabling other processes to access it. Sets system variable result to 0 on success, 1 on failure."],
  ["filewrite", "File operation command", "filewrite <file handle> <data>", "Writes <data> to the file specified by <file handle>. A string is written as-is; an integer is treated as an ASCII code (0-255) and written as the corresponding character."],
  ["filewriteln", "File operation command", "filewriteln <file handle> <data>", "Writes <data> followed by newline characters (CR+LF) to the file. A string is written as-is; an integer is treated as an ASCII code (0-255) and written as the corresponding character."],
  ["findfirst", "File operation command", "findfirst <dir handle> <file name> <strvar>", "Finds the first file matching the <file name> pattern. On success stores a directory handle in <dir handle>, the found filename in <strvar>, and sets result to 1; on failure sets the handle to -1, <strvar> to empty, and result to 0."],
  ["findnext", "File operation command", "findnext <dir handle> <strvar>", "Finds the next file matching the pattern, using the handle from findfirst. Stores the next filename in <strvar> and sets result to 1, or clears <strvar> and sets result to 0 when no more matches exist."],
  ["findclose", "File operation command", "findclose <dir handle>", "Releases the directory handle obtained from findfirst. Should be called after file searching is complete."],
  ["foldercreate", "File operation command", "foldercreate <foldername>", "Creates a new folder named <foldername>. Sets system variable result to 0 on success, non-zero on failure."],
  ["folderdelete", "File operation command", "folderdelete <foldername>", "Deletes the folder specified by <foldername>; it cannot delete a folder containing files. Sets system variable result to 0 on success, non-zero on failure."],
  ["foldersearch", "File operation command", "foldersearch <foldername>", "Searches for the folder specified by <foldername>. Sets result to 1 if it is a folder, 0 otherwise (including for a file), so it cannot reliably check folder existence."],
  ["getdir", "File operation command", "getdir <strvar>", "Stores MACRO's current working directory in <strvar>. The path ends with a backslash only for a root directory."],
  ["getfileattr", "File operation command", "getfileattr <filename>", "Retrieves the attributes of <filename> and stores them in system variable result as bitwise flags (e.g. $1 read-only, $10 directory, $20 archive), or -1 on failure."],
  ["makepath", "File operation command", "makepath <strvar> <dir> <name>", "Builds a full path from directory <dir> and file name <name>, inserting a backslash separator as needed, and stores the result in <strvar>."],
  ["setdir", "File operation command", "setdir <dir>", "Changes MACRO's current working directory to <dir>, affecting subsequent commands like fileopen. This differs from changedir, which sets Tera Term's file transfer directory."],
  ["setfileattr", "File operation command", "setfileattr <filename> <attributes>", "Sets the attributes of <filename> to the combined bitwise <attributes> (e.g. read-only, hidden, archive). Sets system variable result to 1 on success, 0 on failure."],
  ["delpassword", "Password command", "delpassword <filename> <password name>", "Deletes the password identified by <password name> from the password file <filename>. If <password name> is a blank string, all passwords in the file are deleted."],
  ["delpassword2", "Password command", "delpassword2 <filename> <password name>", "Deletes the password identified by <password name> from the password file <filename> created by setpassword2 or getpassword2. If <password name> is blank, all passwords in the file are deleted."],
  ["getpassword", "Password command", "getpassword <filename> <password name> <password var>", "Retrieves the encrypted password named <password name> from <filename>, decrypts it, and stores it in <password var>. Missing files are created; an unknown name prompts the user via a dialog and saves it. Sets result to 1 on success or 0 if the file cannot be written."],
  ["getpassword2", "Password command", "getpassword2 <filename> <password name> <password var> <encrypt str>", "Retrieves the password named <password name> from <filename>, decrypts it using <encrypt str>, and stores it in <password var>. Missing files are created; an unknown name prompts the user and saves it. Sets result to 1 on success or 0 if the file cannot be written."],
  ["ispassword", "Password command", "ispassword <filename> <password name>", "Checks whether the password identified by <password name> is stored in the password file <filename>. Sets result to 1 if found, or 0 if not."],
  ["ispassword2", "Password command", "ispassword2 <filename> <password name>", "Checks whether the password identified by <password name> exists in the password file <filename> created by setpassword2 or getpassword2. Sets result to 1 if found, or 0 if not."],
  ["passwordbox", "Password command", "passwordbox <message> <title> [<special>]", "Opens a dialog prompting the user for a password, displaying the input as asterisks. The entered password is stored in the system variable inputstr."],
  ["setpassword", "Password command", "setpassword <filename> <password name> <password>", "Stores or updates the encrypted <password> identified by <password name> in the password file <filename> (created if it does not exist; multiple entries allowed). Sets result to 1 on success or 0 if the file cannot be written."],
  ["setpassword2", "Password command", "setpassword2 <filename> <password name> <password> <encrypt str>", "Stores or updates <password> identified by <password name> in <filename>, encrypted with <encrypt str> using AES-256-CTR (file created if needed; multiple entries allowed). Sets result to 1 on success or 0 if the file cannot be written."],
  ["beep", "Miscellaneous command", "beep [<sound type>]", "Plays a waveform sound. The optional <sound type> (0-5) selects which system sound; the default is the Windows Default Beep."],
  ["bringupbox", "Miscellaneous command", "bringupbox", "Brings the Tera Term window to the foreground (activates and shows it)."],
  ["checksum8", "Miscellaneous command", "checksum8 <intvar> <string>", "Calculates the 8-bit checksum of <string> and stores the result as a numeric value in <intvar>."],
  ["checksum8file", "Miscellaneous command", "checksum8file <intvar> <filename>", "Calculates the 8-bit checksum of the file <filename> and stores it in <intvar>. If the file cannot be opened, the system variable result is set to -1."],
  ["checksum16", "Miscellaneous command", "checksum16 <intvar> <string>", "Calculates the 16-bit checksum of <string> and stores the result as a numeric value in <intvar>."],
  ["checksum16file", "Miscellaneous command", "checksum16file <intvar> <filename>", "Calculates the 16-bit checksum of the file <filename> and stores it in <intvar>. If the file cannot be opened, the system variable result is set to -1."],
  ["checksum32", "Miscellaneous command", "checksum32 <intvar> <string>", "Calculates the 32-bit checksum of <string> and stores the result as a numeric value in <intvar>."],
  ["checksum32file", "Miscellaneous command", "checksum32file <intvar> <filename>", "Calculates the 32-bit checksum of the file <filename> and stores it in <intvar>. If the file cannot be opened, the system variable result is set to -1."],
  ["clipb2var", "Miscellaneous command", "clipb2var <strvar> [<offset>]", "Copies clipboard text into <strvar>, optionally starting at a chunk given by <offset>. Sets result: 0 failure, 1 success, 2 truncated, 3 memory error."],
  ["closesbox", "Miscellaneous command", "closesbox", "Closes the status dialog box that was opened by the statusbox command."],
  ["crc16", "Miscellaneous command", "crc16 <intvar> <string>", "Calculates the CRC-16 of <string> (polynomial x16+x12+x5+x0) and stores the result as a numeric value in <intvar>."],
  ["crc16file", "Miscellaneous command", "crc16file <intvar> <filename>", "Calculates the CRC-16 of the file <filename> and stores it in <intvar>. If the file cannot be opened, the system variable result is set to -1."],
  ["crc32", "Miscellaneous command", "crc32 <intvar> <string>", "Calculates the CRC-32 of <string> (Ethernet FCS polynomial) and stores the result as a numeric value in <intvar>."],
  ["crc32file", "Miscellaneous command", "crc32file <intvar> <filename>", "Calculates the CRC-32 of the file <filename> and stores it in <intvar>. If the file cannot be opened, the system variable result is set to -1."],
  ["dirnamebox", "Miscellaneous command", "dirnamebox <title> [<initialdir>]", "Opens a dialog to select a directory. The chosen directory name is stored in the system variable inputstr."],
  ["exec", "Miscellaneous command", "exec <command line> [<show> [<wait> [<current directory>]]]", "Runs an application specified by <command line>, optionally controlling the window show state, whether to wait for it, and the working directory."],
  ["filenamebox", "Miscellaneous command", "filenamebox <title> [<dialogtype> [<initialdir>]]", "Opens a file selection dialog. The chosen filename is stored in the system variable inputstr, and result is set nonzero on selection or zero if canceled."],
  ["getdate", "Miscellaneous command", "getdate <strvar> [<format> [<timezone>]]", "Stores the current date in <strvar>, default format YYYY-MM-DD (%Y-%m-%d). <format> uses strftime syntax; an optional <timezone> overrides local time."],
  ["getenv", "Miscellaneous command", "getenv <envname> <strvar>", "Retrieves the value of the environment variable named <envname> and stores it in the string variable <strvar>."],
  ["getipv4addr", "Miscellaneous command", "getipv4addr <string array> <intvar>", "Retrieves the local IPv4 address(es) into <string array> and the count into <intvar>. Sets result: 1 success, 0 array overflow, -1 error."],
  ["getipv6addr", "Miscellaneous command", "getipv6addr <string array> <intvar>", "Retrieves the local IPv6 address(es) into <string array> and the count into <intvar>. Sets result: 1 success, 0 array too small, -1 failure."],
  ["getspecialfolder", "Miscellaneous command", "getspecialfolder <strvar> <foldertype>", "Stores the path of the Windows special folder identified by <foldertype> into the string variable <strvar>."],
  ["gettime", "Miscellaneous command", "gettime <strvar> [<format> [<timezone>]]", "Stores the current time in <strvar>, default format HH:MM:SS (%H:%M:%S). <format> uses strftime syntax; an optional <timezone> overrides local time."],
  ["getttdir", "Miscellaneous command", "getttdir <strvar>", "Stores the ttpmacro.exe directory into the string variable <strvar>."],
  ["getver", "Miscellaneous command", "getver <strvar> [<version>]", "Stores the Tera Term version string in <strvar>. If <version> is given, it is compared and result is set: -2 invalid, -1 older, 0 equal, 1 newer."],
  ["ifdefined", "Miscellaneous command", "ifdefined <var>", "Sets the system variable result to the type of <var>: 0 undefined, 1 integer, 3 string, 4 label, 5 integer array, 6 string array."],
  ["inputbox", "Miscellaneous command", "inputbox <message> <title> [<default> [<special>]]", "Displays a dialog prompting the user to enter a string. The entered text is stored in the system variable inputstr; <default> sets initial text."],
  ["intdim", "Miscellaneous command", "intdim <array> <size>", "Declares an integer array <array> with <size> entries (1 to 65536, 0-origin index). Default element value is 0."],
  ["listbox", "Miscellaneous command", "listbox <message> <title> <string array> [<selected>] [<keyword parameter>...]", "Displays a list box of items from <string array>. The selected index (0 to N-1) is stored in result, or -1 if canceled; <selected> pre-selects an item."],
  ["messagebox", "Miscellaneous command", "messagebox <message> <title> [<special>]", "Displays a dialog box showing <message> with the given <title>. A nonzero <special> enables escape-sequence interpretation (obsolete; use strspecial)."],
  ["random", "Miscellaneous command", "random <integer variable> <max number>", "Generates a random integer in the range 0..<max number> and assigns it to <integer variable>. <max number> must be greater than 0."],
  ["rotateleft", "Miscellaneous command", "rotateleft <intvar> <intval> <count>", "Circularly shifts <intval> left by <count> bits and stores the result in the integer variable <intvar>."],
  ["rotateright", "Miscellaneous command", "rotateright <intvar> <intval> <count>", "Circularly shifts <intval> right by <count> bits and stores the result in the integer variable <intvar>."],
  ["setdate", "Miscellaneous command", "setdate <date>", "Sets the system date to <date>, which must be in the format YYYY-MM-DD."],
  ["setdlgpos", "Miscellaneous command", "setdlgpos [<x> <y> [<position> [<offset x> <offset y>]]]", "Sets the display position (in pixels) of dialog boxes such as inputbox, listbox, messagebox, statusbox, and yesnobox. With no arguments, dialogs are centered on the primary display."],
  ["setenv", "Miscellaneous command", "setenv <envname> <strval>", "Sets the environment variable <envname> to <strval> within the macro environment only (not the system), overwriting it if it already exists."],
  ["setexitcode", "Miscellaneous command", "setexitcode <exit code>", "Sets the macro process exit code, which can be tested afterward via the DOS 'if errorlevel n' command."],
  ["settime", "Miscellaneous command", "settime <time>", "Sets the system time to <time>, which must be in the format HH:MM:SS."],
  ["show", "Miscellaneous command", "show <show flag>", "Controls the macro window: minimizes it if <show flag> is 0, restores and brings it to top if greater than 0, and hides it if less than 0."],
  ["statusbox", "Miscellaneous command", "statusbox <message> <title> [<special>]", "Displays a status dialog box (or updates its <message> and <title> if already shown). Position it with setdlgpos and close it with closesbox; it cannot be dismissed by the user."],
  ["strdim", "Miscellaneous command", "strdim <array> <size>", "Declares a string array <array> with <size> entries (1 to 65536, 0-origin index). Default element value is an empty string."],
  ["uptime", "Miscellaneous command", "uptime <intvar>", "Stores into <intvar> the number of milliseconds elapsed since Windows started. The value wraps to zero after about 49.7 days of continuous running."],
  ["var2clipb", "Miscellaneous command", "var2clipb <string>", "Copies <string> to the clipboard. Sets the system variable result to 0 if the clipboard could not be opened, or 1 on success."],
  ["yesnobox", "Miscellaneous command", "yesnobox <message> <title> [<special>]", "Displays a Yes/No dialog box with <message> and <title>. The system variable result is set to 1 if Yes is clicked, or 0 if No."],
];
